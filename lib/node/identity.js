/**
 * Returns the input argument without any modifications.
 *
 * This identity function takes any value and returns it unchanged.
 *
 * @template T
 * @param {T} x - The value to be returned.
 * @returns {T} The same value that was provided as input.
 */
const identity = x => x;

export { identity };